import {getJSON} from './lib/core-store.mjs';
const env=name=>globalThis.Netlify?.env?.get?.(name)||process.env[name];
export default async()=>{const cb=await getJSON('safeplate_circuit',{failures:0,openUntil:null}),feedback=await getJSON('safeplate_feedback_latest',null),last=cb.lastSuccess?new Date(cb.lastSuccess).getTime():0,ageMinutes=last?Math.floor((Date.now()-last)/60_000):null,enabled=env('VERISCOPE_SAFEPLATE_ENABLED')!=='false',feedbackEnabled=Boolean(env('SAFEPLATE_FINDINGS_URL')&&env('VERISCOPE_SAFEPLATE_FINDINGS_TOKEN')),operational=enabled&&ageMinutes!=null&&ageMinutes<=30&&!cb.openUntil,status=!enabled?'UNAVAILABLE':operational?'ONLINE':'DEGRADED';return Response.json({service:'VERISCOPE CORE',version:'1.3.0-reviewed-bridge',mode:'SHADOW',status,operational,scoreStatus:'UNVALIDATED_SHADOW_SCORE',autoMergeEnabled:false,humanReviewRequired:true,surveillance:{targetMinutes:15,lastSuccessfulCycle:cb.lastSuccess||null,ageMinutes,nextExpectedBy:cb.lastSuccess?new Date(last+15*60_000).toISOString():null},safeplateAdapter:{enabled,state:operational?'TESTED':'CONNECTED_NOT_VERIFIED',circuitBreaker:cb},safeplateFeedback:{enabled:feedbackEnabled,state:feedback?.status||'READY',lastDelivery:feedback?.deliveredAt||null,policy:'HUMAN_APPROVED_ONLY',retryMinutes:15},timestamp:new Date().toISOString()},{headers:{'cache-control':'no-store','x-content-type-options':'nosniff'}})};
export const config={path:'/api/health'};
