[build]
  command = "npm test"
  publish = "."

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."safeplate-sync"]
  schedule = "*/15 * * * *"
